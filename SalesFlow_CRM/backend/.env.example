PORT=3001
FRONTEND_URL=http://localhost:3200

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your_postgresql_password
DB_NAME=crm_sales_pipeline
DB_SYNC=true

SESSION_SECRET=change_this_to_a_long_random_secret

# Optional email. Leave blank and the app will continue without sending email.
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=
MAIL_PASSWORD=
MAIL_FROM=CRM Sales Pipeline <no-reply@example.com>

# Optional Pusher. Leave blank and the app will continue without realtime notifications.
PUSHER_APP_ID=
PUSHER_KEY=
PUSHER_SECRET=
PUSHER_CLUSTER=ap2

SEED_ADMIN_NAME=CRM Admin
SEED_ADMIN_EMAIL=admin@crm.local
SEED_ADMIN_PASSWORD=Admin123!
