'use client';
import Link from 'next/link';import { usePathname } from 'next/navigation';import { BarChart3,Building2,CalendarCheck,Handshake,LayoutDashboard,LogOut,PanelTop,UserCircle,Users,UsersRound,Bell } from 'lucide-react';import { useAuth } from './AuthProvider';
const items=[['/dashboard', 'Dashboard',LayoutDashboard],['/dashboard/companies','Companies',Building2],['/dashboard/contacts','Contacts',Users],['/dashboard/leads','Leads',UsersRound],['/dashboard/deals','Deals',Handshake],['/dashboard/pipeline','Pipeline',PanelTop],['/dashboard/activities','Activities',CalendarCheck],['/dashboard/notifications','Notifications',Bell],['/dashboard/profile','Profile',UserCircle]] as const;
export default function Sidebar(){const path=usePathname();const{user,logout}=useAuth();return <aside className="fixed left-0 top-0 hidden h-screen w-64 flex-col bg-slate-950 text-white lg:flex"><div className="border-b border-white/10 p-6"><p className="text-xs font-bold tracking-[.25em] text-slate-400">SALESFLOW</p><h2 className="mt-2 text-xl font-bold">CRM Workspace</h2></div><nav className="flex-1 space-y-1 p-4">{items.map(([href,label,Icon])=>{const active=href==='/dashboard'?path===href:path.startsWith(href);return <Link key={href} href={href} className={`flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm ${active?'bg-white text-slate-950':'text-slate-300 hover:bg-white/10 hover:text-white'}`}><Icon size={18}/>{label}</Link>})}{(user?.role==='ADMIN'||user?.role==='MANAGER')&&<Link href="/dashboard/users" className={`flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm ${path.startsWith('/dashboard/users')?'bg-white text-slate-950':'text-slate-300 hover:bg-white/10'}`}><BarChart3 size={18}/>Team Users</Link>}</nav><div className="border-t border-white/10 p-4"><div className="mb-3 px-2"><p className="font-semibold">{user?.name}</p><p className="text-xs text-slate-400">{user?.role}</p></div><button onClick={logout} className="flex w-full items-center gap-3 rounded-xl px-3 py-2 text-sm text-slate-300 hover:bg-white/10"><LogOut size={18}/>Logout</button></div></aside>}
